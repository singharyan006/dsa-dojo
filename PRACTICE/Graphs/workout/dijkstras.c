#include <stdio.h>
#include <limits.h>

#define V 5

int minDist(int dist[], int visited[]) {
    int minDistance = INT_MAX, minVertex;
    for(int vertex = 0; vertex < V; vertex++) {
        
    } 
}